<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
			 xmlns:lang="clr-namespace:FileDB.Lang"
			 xmlns:vm="clr-namespace:FileDB.ViewModels.Search.File"
             x:Class="FileDB.Views.Search.File.FileCategorizationPersonsView"
             IsVisible="{Binding IsExpanded}">
    <Expander Header="{Binding UpdatePersonsHeader}" IsExpanded="{Binding UpdatePersonsIsExpanded}">
		<Grid ColumnDefinitions="auto" RowDefinitions="auto,*">
			<StackPanel Grid.Row="0" Orientation="Horizontal" IsVisible="{Binding FileSelected}">
				<TextBlock Text="&#x1F50D;" Margin="0 0 5 0" VerticalAlignment="Center"/>
				<TextBox Width="125" Text="{Binding PersonsFilterText}" Margin="0 0 5 0" VerticalAlignment="Center"/>
				<Button Content="&#128473;" IsEnabled="{Binding PersonsFilterText, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" VerticalAlignment="Center" Command="{Binding ClearPersonsFilterTextCommand}"/>
				<Button Content="&#9212;" Margin="10,0,0,0" IsEnabled="{Binding HasVisiblePersons}" VerticalAlignment="Center" Command="{Binding TogglePersonsCommand}" ToolTip.Tip="{x:Static lang:Strings.CategorizationUpdatePersonsToggleAllToolTip}"/>
				<Button Content="+" Margin="10,0,0,0" VerticalAlignment="Center" Command="{Binding CreatePersonCommand}" ToolTip.Tip="{x:Static lang:Strings.CategorizationCreateANewPersonAndAddItToCurrentFileToolTip}"/>
			</StackPanel>

            <TextBlock Grid.Row="1" IsVisible="{Binding !Persons.Count}" Text="{x:Static lang:Strings.CategorizationUpdatePersonsNoPersons}"/>
			<ScrollViewer Grid.Row="1" Margin="0,10,0,0">
				<ItemsControl ItemsSource="{Binding Persons}" IsVisible="{Binding FileSelected}">
					<ItemsControl.ItemTemplate>
						<DataTemplate x:DataType="vm:PersonToToggleViewModel">
							<ToggleButton IsVisible="{Binding IsVisible}" Command="{Binding $parent[ItemsControl].DataContext.TogglePersonCommand}" CommandParameter="{Binding}" Classes="ColoredToggleButton" Margin="0,2,0,2" Content="{Binding ShortName}" IsChecked="{Binding IsChecked, Mode=OneWay}" ToolTip.Tip="{Binding Name}" />
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</ScrollViewer>
		</Grid>
	</Expander>
</UserControl>
