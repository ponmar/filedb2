<UserControl x:Class="FileDB.View.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileDB.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Label DockPanel.Dock="Top" Style="{StaticResource PageHeading}">Import</Label>
        <ScrollViewer>
            <StackPanel Orientation="Vertical" Width="auto" Style="{StaticResource PageStackPanel}">
                <TextBlock Style="{StaticResource PageTextBlock}">&#x26A0; Check your settings to adjust what files will be listed.</TextBlock>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource NormalButton}" Width="100" Margin="10" Command="{Binding ScanNewFilesCommand}">Find files...</Button>
                    <Button Style="{StaticResource NormalButton}" Width="100" Margin="10" Command="{Binding ImportNewFilesCommand}" IsEnabled="{Binding NewFilesAvailable}">Import listed files</Button>
                </StackPanel>
                <DataGrid Margin="10" ItemsSource="{Binding NewFiles}" GridLinesVisibility="Horizontal" CanUserResizeColumns="False" CanUserResizeRows="False" CanUserAddRows="false" CanUserDeleteRows="false" IsReadOnly="True"/>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
