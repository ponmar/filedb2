<UserControl x:Class="FileDB.View.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileDB.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Label DockPanel.Dock="Top" Style="{StaticResource PageHeading}">Tools</Label>
        <ScrollViewer>
            <StackPanel Orientation="Vertical">
                <Label Style="{StaticResource PageSubHeading}">Database Backup</Label>
                <StackPanel Orientation="Vertical" Style="{StaticResource PageStackPanel}">
                    <TextBlock Style="{StaticResource PageTextBlock}" TextWrapping="Wrap">&#x26A0; Backup of your files collection, FileDB configuration and FileDB databases to external media should be done continuously!</TextBlock>
                    <TextBlock Style="{StaticResource PageTextBlock}" TextWrapping="Wrap">&#x26A0; Database recovery must be done manually if needed.</TextBlock>
                    <Button Margin="10" Width="200" Style="{StaticResource NormalButton}" Command="{Binding CreateBackupCommand}" HorizontalAlignment="Left">Create database backup</Button>
                    <TextBlock Style="{StaticResource PageTextBlock}" HorizontalAlignment="Left" Text="Backup files in configured database directory:"/>
                    <ListBox Margin="10" ItemsSource="{Binding BackupFiles}" HorizontalAlignment="Left">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Path=Filename}" ToolTip="{Binding Path=Filename}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
