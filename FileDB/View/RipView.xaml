<UserControl x:Class="FileDB.View.RipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FileDB.View"
             xmlns:rs="clr-namespace:FileDB.Resources"
             xmlns:converters="clr-namespace:FileDB.Converters"
             xmlns:viewmodel="clr-namespace:FileDB.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:RipViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:EnumerationToVisibilityConverter x:Key="EnumerationToVisibilityConverter" />
        <converters:ValueToVisibilityConverter x:Key="ValueToVisibilityConverter" />

        <DataTemplate x:Key="PersonTemplate">
            <Border Style="{StaticResource BorderGroup}">
                <StackPanel Orientation="Horizontal" Margin="5" Width="400">
                    <Image Visibility="{Binding ProfilePicture, Converter={StaticResource ValueToVisibilityConverter}}" Width="100" Height="100" Margin="5" Source="{Binding ProfilePicture}" RenderOptions.BitmapScalingMode="Fant" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Vertical" Margin="5" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Margin="5" Style="{StaticResource PageSubHeading}"/>
                            <TextBlock Text="{Binding Age}" Margin="5" Style="{StaticResource PageSubHeading}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource PageTextBlock}" Text="{Binding DateOfBirth}" Margin="5"/>
                            <TextBlock Style="{StaticResource PageTextBlock}" Text="-" Margin="5"/>
                            <TextBlock Style="{StaticResource PageTextBlock}" Text="{Binding DeceasedStr}" Margin="5"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Style="{StaticResource PageHeading}" Text="{x:Static rs:Strings.RipHeading}"/>
        <ScrollViewer PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <StackPanel Style="{StaticResource PageStackPanel}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource PageInnerTextBlock}" Text="{x:Static rs:Strings.RipFilterLabel}" Margin="10" VerticalAlignment="Center"/>
                    <TextBox Width="200" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                </StackPanel>
                <ListView Visibility="{Binding Persons, Converter={StaticResource EnumerationToVisibilityConverter}}" Background="Transparent" BorderThickness="0" Margin="10" ItemsSource="{Binding Persons}" ItemTemplate="{StaticResource PersonTemplate}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
