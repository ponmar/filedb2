<UserControl x:Class="FileDB.View.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FileDB.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Label DockPanel.Dock="Top" Style="{StaticResource PageHeading}">About</Label>
        <ScrollViewer>
            <StackPanel Orientation="Vertical">
                <Label Style="{StaticResource PageSubHeading}">Releases</Label>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0">Download:</TextBlock>
                    <TextBlock Grid.Row="0" Grid.Column="1">
                        <Hyperlink NavigateUri="{Binding DownloadLink}" RequestNavigate="OpenUri">
                            <TextBlock Text="Click" ToolTip="{Binding DownloadLink}"/>
                        </Hyperlink>
                    </TextBlock>
                </Grid>

                <Label Style="{StaticResource PageSubHeading}">Changes</Label>
                <TextBlock Text="{Binding Changes}"/>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
