<UserControl x:Class="FileDB.View.FindView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FileDB.View"
             xmlns:converters="clr-namespace:FileDB.Converters"
             xmlns:viewmodel="clr-namespace:FileDB.ViewModel"
             xmlns:view="clr-namespace:FileDB.View"
             d:DataContext="{d:DesignInstance Type=viewmodel:FindViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1000"
             Focusable="True">
    <UserControl.InputBindings>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="1" Key="F1"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="2" Key="F2"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="3" Key="F3"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="4" Key="F4"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="5" Key="F5"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="6" Key="F6"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="7" Key="F7"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="8" Key="F8"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="9" Key="F9"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="10" Key="F10"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="11" Key="F11"/>
        <KeyBinding Command="{Binding FunctionKeyCommand}" CommandParameter="12" Key="F12"/>
        <KeyBinding Command="{Binding PrevFileCommand}" Key="Left"/>
        <KeyBinding Command="{Binding NextFileCommand}" Key="Right"/>
        <KeyBinding Command="{Binding FirstFileCommand}" Key="Home"/>
        <KeyBinding Command="{Binding LastFileCommand}" Key="End"/>
        <KeyBinding Command="{Binding NextDirectoryCommand}" Key="PageDown"/>
        <KeyBinding Command="{Binding PrevDirectoryCommand}" Key="PageUp"/>
        <!--<KeyBinding Command="{Binding EscKeyCommand}" Key="F" Modifiers="Alt"/>-->
    </UserControl.InputBindings>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <converters:NegatedBooleanToVisibilityConverter x:Key="NegatedBooleanToVisibilityConverter"/>
        <converters:EnumerationToVisibilityConverter x:Key="EnumerationToVisibilityConverter"/>
        <converters:EnumerationEmptyToVisibilityConverter x:Key="EnumerationEmptyToVisibilityConverter"/>
        <converters:StringContentToBoolConverter x:Key="StringContentToBoolConverter"/>
    </UserControl.Resources>

    <Grid Margin="0 0 0 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Visibility="{Binding Maximize, Converter={StaticResource NegatedBooleanToVisibilityConverter}}">
            <TextBlock Style="{StaticResource PageHeading}">Search</TextBlock>
            <view:SearchCriteria/>
            <view:FileBrowseControls/>
        </StackPanel>

        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <!-- Image or error message -->
            <Border Style="{StaticResource BorderGroup}" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Grid.RowSpan="2" Margin="0 10 0 0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Style="{StaticResource PageInnerTextBlock}" Text="{Binding CurrentFileLoadError}" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding CurrentFileLoadError, Converter={converters:StringNullOrEmptyToVisibilityConverter}}"/>
                    <Image x:Name="CurrentFileImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant"/>
                </Grid>
            </Border>

            <!-- Left side areas when not maximized -->
            <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Margin="10 22 0 0" Width="235" Visibility="{Binding Maximize, Converter={StaticResource NegatedBooleanToVisibilityConverter}}">
                <view:FileInfo/>
            </StackPanel>

            <!-- Left side areas when maximized -->
            <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Margin="10 22 0 0" Visibility="{Binding Maximize, Converter={StaticResource BoolToVis}}">
                <view:FileInfoMaximized/>
            </StackPanel>

            <!-- Right side areas -->
            <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Margin="0 22 10 0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                    <Button Margin="0,0,5,0" Width="50" Command="{Binding OpenFileLocationCommand}" Style="{StaticResource NormalButton}" IsEnabled="{Binding HasNonEmptySearchResult}" ToolTip="Locate file in Explorer" Visibility="{Binding Maximize, Converter={StaticResource NegatedBooleanToVisibilityConverter}}">Locate</Button>
                    <Button Margin="0,0,5,0" Width="50" Command="{Binding OpenFileWithDefaultAppCommand}" Style="{StaticResource NormalButton}" IsEnabled="{Binding HasNonEmptySearchResult}" ToolTip="Open file with default application" Visibility="{Binding Maximize, Converter={StaticResource NegatedBooleanToVisibilityConverter}}">Open</Button>
                    <Button Margin="0,0,5,0" Width="50" Command="{Binding CopyFileIdCommand}" Style="{StaticResource NormalButton}" IsEnabled="{Binding HasNonEmptySearchResult}" ToolTip="Copy file id" Visibility="{Binding Maximize, Converter={StaticResource NegatedBooleanToVisibilityConverter}}">Copy</Button>
                    <ToggleButton Grid.RowSpan="2" Margin="0" Style="{StaticResource NavigationToggleButton}" IsChecked="{Binding Maximize}" Content="&#x1F5B5;" ToolTip="Maximize" IsEnabled="{Binding HasNonEmptySearchResult}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Visibility="{Binding Maximize, Converter={StaticResource NegatedBooleanToVisibilityConverter}}">
                    <view:FileCategorizationHistory VerticalAlignment="Top"/>
                    <view:FileCategorization VerticalAlignment="Top"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Bottom file info when maximized -->
            <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" HorizontalAlignment="Center" Visibility="{Binding Maximize, Converter={StaticResource BoolToVis}}">
                <Border Style="{StaticResource MaximizedBorderGroup}" Margin="20 0 20 20" Padding="10" Visibility="{Binding CurrentFileDescription, Converter={converters:StringNullOrEmptyToVisibilityConverter}}">
                    <TextBlock Style="{StaticResource PageInnerSubHeading}" Text="{Binding CurrentFileDescription}" ToolTip="{Binding CurrentFileDescription}" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
